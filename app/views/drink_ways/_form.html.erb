<%= form_with(model: drink_way) do |form| %>
  <% if drink_way.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(drink_way.errors.count, "error") %> prohibited this drink_way from being saved:</h2>

      <ul>
        <% drink_way.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :how_to_make %>
    <%= form.text_area :how_to_make %>
  </div>

  <div class="field">
    <%= form.label :explantion %>
    <%= form.text_area :explantion %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
